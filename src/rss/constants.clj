(ns rss.constants
  )

;; Environment variable name used to read the config path.
(def rss-config-path-key "RSS_CONFIG_PATH")

;; Environment variable name indicating we should use a Resource Principal.
(def resource-principal-key "OCI_RESOURCE_PRINCIPAL_RPST")

;; Environment variable name indicating we should use an Instance Principal.
(def instance-principal-key "OCI_INSTANCE_PRINCIPAL_IPST")